rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/progress_photos/{allPaths=**} {
      // Solo el usuario puede subir o leer sus propias fotos de progreso
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
